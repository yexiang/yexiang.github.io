name: 'GitHub Actions Weather Bot'
 
on:
  push:
  schedule:
    - cron: '20 * * * *'


jobs:
  # This workflow contains a single job called "build"
  build:
    runs-on: ubuntu-latest
    steps:
    - name: 'Checkout codes'
      uses: actions/checkout@v1

    - name: 'Get weather report'
      run:  mkdir yy && curl "https://wttr.in/Guicheng?format=4&m" -o /home/runner/work/yexiang.github.io/yexiang.github.io/yy/guicheng.html

    - name: 'push html'
      run:  git config --global user.email "ye.123789456@gmail.com" && git config --global user.name "yexiang" && git clone https://github.com/yexiang/yexiang.github.io yy && cd yy &&  git add . && git commit -m "initial commit" && git push origin master   

